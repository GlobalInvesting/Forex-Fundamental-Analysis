name: Update Extended Data (Bonds + Sentiment + Flows) - FINAL v3
on:
  schedule:
    - cron: '30 6 * * *'
  workflow_dispatch:

jobs:
  update-extended-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 lxml --break-system-packages

      - name: Create directory
        run: mkdir -p extended-data

      - name: Scrape extended indicators
        run: |
          python3 << 'EOFPYTHON'
          import requests
          from bs4 import BeautifulSoup
          import json, re, os, time
          from datetime import date, datetime

          HEADERS = {
              'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
              'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,*/*;q=0.8',
          }

          CURRENCIES = ['USD', 'EUR', 'GBP', 'JPY', 'CAD', 'AUD', 'CHF', 'NZD']

          COUNTRY_NAMES = {
              'USD': ['United States'],
              'EUR': ['Euro Area'],
              'GBP': ['United Kingdom'],
              'JPY': ['Japan'],
              'CAD': ['Canada'],
              'AUD': ['Australia'],
              'CHF': ['Switzerland'],
              'NZD': ['New Zealand']
          }

          # ── CRITICAL FIX: ?continent=world en TODAS las URLs ──────────────
          # Sin este parámetro, TE muestra un subconjunto de países por región
          # y NZD no aparece en businessConfidence (ni en otros indicadores de Oceanía).
          # Con ?continent=world se garantiza que el scraper ve TODOS los países.
          TE_URLS = {
              'bond10y':               'https://tradingeconomics.com/country-list/government-bond-yield?continent=world',
              'consumerConfidence':    'https://tradingeconomics.com/country-list/consumer-confidence?continent=world',
              'businessConfidence':    'https://tradingeconomics.com/country-list/business-confidence?continent=world',
              'capitalFlows':          'https://tradingeconomics.com/country-list/capital-flows?continent=world',
              'fdi':                   'https://tradingeconomics.com/country-list/foreign-direct-investment?continent=world',
              'inflationExpectations': 'https://tradingeconomics.com/country-list/inflation-expectations?continent=world',
          }

          def clean_num(text):
              if not text: return None
              text = str(text).strip().replace(',','').replace('%','')
              m = re.search(r'(-?\d+\.?\d*)', text)
              return float(m.group(1)) if m else None

          def parse_te_date(date_text):
              if not date_text: return str(date.today())
              date_text = date_text.strip()
              try:
                  if re.search(r'[A-Za-z]{3}\s*[/\s]\s*\d{2,4}', date_text):
                      m = re.search(r'([A-Za-z]{3})\s*[/\s]\s*(\d{2,4})', date_text)
                      if m:
                          yr = m.group(2)
                          if len(yr) == 2: yr = '20' + yr
                          dt = datetime.strptime(f"{m.group(1)} {yr}", '%b %Y')
                          return dt.strftime('%Y-%m-15')
                  if re.search(r'Q\d[/\s]\d{4}', date_text):
                      m = re.search(r'Q(\d)[/\s](\d{4})', date_text)
                      if m:
                          month = (int(m.group(1)) - 1) * 3 + 2
                          return f"{m.group(2)}-{month:02d}-15"
              except: pass
              return str(date.today())

          def normalize_confidence(value, currency, indicator):
              """
              Normaliza índices de confianza a escala base-100 (100 = neutral).

              ┌─────────┬──────────────────────────────────┬─────────────────┬──────────────────────────────────┐
              │Currency │ Índice                           │ Escala original │ Normalización                    │
              ├─────────┼──────────────────────────────────┼─────────────────┼──────────────────────────────────┤
              │ USD     │ Conference Board / Michigan      │ 0-150+          │ Pass-through                     │
              │ EUR     │ European Commission (ambos)      │ -100/+100       │ +100 shift                       │
              │ GBP     │ GfK Consumer / CBI (ambos)      │ -100/+100       │ +100 shift                       │
              │ JPY     │ Cabinet Office (consumer)        │ 35-50 range     │ fórmula ((v-40)/10)*15+100       │
              │ JPY     │ Tankan Large Mfg (business)      │ -100/+100       │ +100 shift                       │
              │ AUD     │ Westpac/ANZ (consumer)           │ ~100 base       │ Pass-through                     │
              │ AUD     │ NAB Business Confidence (biz)   │ -100/+100       │ +100 shift                       │
              │ CAD     │ Conference Board Canada (con)   │ ~100 base       │ Pass-through                     │
              │ CAD     │ Ivey PMI (business)              │ 0-100           │ Pass-through                     │
              │ CHF     │ SECO (consumer)                  │ ~-150 a +50     │ +130 shift (neutral ≈ -30 → 100) │
              │ CHF     │ KOF Barometer (business)         │ ~100 base       │ Pass-through                     │
              │ NZD     │ ANZ-Roy Morgan (consumer)        │ ~100 base       │ Pass-through                     │
              │ NZD     │ ANZ Business Outlook (business)  │ -100/+100       │ +100 shift                       │
              └─────────┴──────────────────────────────────┴─────────────────┴──────────────────────────────────┘
              """
              if value is None:
                  return None

              if currency == 'EUR' and indicator in ['consumerConfidence', 'businessConfidence']:
                  return round(value + 100, 2)

              if currency == 'GBP' and indicator in ['consumerConfidence', 'businessConfidence']:
                  return round(value + 100, 2)

              if currency == 'JPY' and indicator == 'consumerConfidence':
                  return round(((value - 40) / 10) * 15 + 100, 2)

              if currency == 'JPY' and indicator == 'businessConfidence':
                  return round(value + 100, 2)

              if currency == 'AUD' and indicator == 'businessConfidence':
                  return round(value + 100, 2)

              if currency == 'CHF' and indicator == 'consumerConfidence':
                  # SECO: promedio histórico long-run ≈ -30 → se mapea a 100
                  return round(value + 130, 2)

              if currency == 'NZD' and indicator == 'businessConfidence':
                  return round(value + 100, 2)

              return value

          def scrape_te_table(url, label):
              print(f"\n{'='*50}\nSCRAPING: {label}\n{'='*50}")
              data, dates = {}, {}
              try:
                  r = requests.get(url, headers=HEADERS, timeout=20)
                  if r.status_code == 429:
                      print(f"  ⚠️ Rate limited, waiting 30s...")
                      time.sleep(30)
                      r = requests.get(url, headers=HEADERS, timeout=20)
                  r.raise_for_status()

                  soup = BeautifulSoup(r.content, 'lxml')
                  table = soup.find('table', {'class': 'table'})
                  if not table:
                      print("  ❌ No table found"); return data, dates

                  headers_row = [h.get_text(strip=True).lower() for h in table.find_all('th')]
                  actual_idx = headers_row.index('actual') if 'actual' in headers_row else 1
                  date_idx   = headers_row.index('reference') if 'reference' in headers_row else None

                  for row in table.find_all('tr')[1:]:
                      cols = row.find_all('td')
                      if len(cols) < 2: continue
                      ctry = cols[0].get_text(strip=True)
                      for code, names in COUNTRY_NAMES.items():
                          if any(n.lower() in ctry.lower() for n in names):
                              val = clean_num(cols[actual_idx].get_text(strip=True))
                              dt  = parse_te_date(cols[date_idx].get_text(strip=True)) if date_idx and len(cols) > date_idx else str(date.today())
                              if val is not None:
                                  if label in ['consumerConfidence', 'businessConfidence']:
                                      val = normalize_confidence(val, code, label)
                                  data[code] = round(val, 4)
                                  dates[code] = dt
                                  print(f"  ✓ {code}: {val} ({dt})")
                              break
              except Exception as e:
                  print(f"  ❌ {e}")
              return data, dates

          # ── BOND YIELDS ────────────────────────────────────────────────────────
          #
          # Bugs corregidos:
          #   1. ?continent=world → ahora NZD aparece en la lista
          #   2. El fallback de página individual capturaba "10" (de "10 Year Bond")
          #      en vez del yield real. Se eliminó ese fallback y se mejoró el
          #      escáner de widget-tables que ahora es más que suficiente con world.

          BOND_COUNTRY_MAP = {
              'USD': ['United States'],
              'EUR': ['Germany'],       # EUR proxy = Bund alemán
              'GBP': ['United Kingdom'],
              'JPY': ['Japan'],
              'CAD': ['Canada'],
              'AUD': ['Australia'],
              'CHF': ['Switzerland'],
              'NZD': ['New Zealand'],
          }

          BOND_COUNTRY_SLUGS = {
              'USD': 'united-states',
              'EUR': 'germany',
              'GBP': 'united-kingdom',
              'JPY': 'japan',
              'CAD': 'canada',
              'AUD': 'australia',
              'CHF': 'switzerland',
              'NZD': 'new-zealand',
          }

          # Rangos de yield razonables por divisa (para filtrar falsos positivos en fallback)
          BOND_EXPECTED_RANGES = {
              'USD': (3.0, 6.0),
              'EUR': (1.5, 4.0),  # Germany Bund
              'GBP': (3.5, 5.5),
              'JPY': (0.5, 3.5),
              'CAD': (2.5, 5.0),
              'AUD': (3.5, 6.0),
              'CHF': (0.0, 2.0),
              'NZD': (3.5, 6.0),  # NZ 10Y típicamente 4.0-5.5%
          }

          def _extract_bonds_from_all_tables(soup):
              """Escanea todas las tablas buscando yields válidos (0 < v < 20)."""
              data, dates = {}, {}
              for table in soup.find_all('table'):
                  for row in table.find_all('tr'):
                      cols = row.find_all('td')
                      if len(cols) < 2: continue
                      ctry_text = cols[0].get_text(strip=True)
                      for code, names in BOND_COUNTRY_MAP.items():
                          if code in data: continue
                          if any(n.lower() in ctry_text.lower() for n in names):
                              for idx in [1, 2, 3]:
                                  if idx >= len(cols): continue
                                  val = clean_num(cols[idx].get_text(strip=True))
                                  if val is not None and 0 < val < 20:
                                      data[code]  = round(val, 3)
                                      dates[code] = str(date.today())
                                      print(f"  ✓ {code}: {val}% (widget table)")
                                      break
              return data, dates

          def _bond_country_page_fallback(code):
              """
              Fallback a página individual — MEJORADO para evitar capturar
              el número '10' de 'Government Bond Yield - 10 Year' en el título.
              Solo acepta valores dentro del rango esperado para ese país.
              """
              slug = BOND_COUNTRY_SLUGS.get(code)
              if not slug: return None, None
              min_val, max_val = BOND_EXPECTED_RANGES.get(code, (0.5, 10.0))

              url = f'https://tradingeconomics.com/{slug}/government-bond-yield'
              try:
                  r = requests.get(url, headers=HEADERS, timeout=15)
                  r.raise_for_status()
                  soup = BeautifulSoup(r.content, 'lxml')

                  # Buscar primero en elementos específicos de yield display
                  # TE muestra el valor actual en un elemento con clase 'datatable-item'
                  # o en el primer <td> de las tablas de datos del país
                  for table in soup.find_all('table'):
                      for row in table.find_all('tr'):
                          cols = row.find_all('td')
                          for col in cols:
                              val = clean_num(col.get_text(strip=True))
                              # Filtro estricto: solo valores dentro del rango esperado
                              if val is not None and min_val <= val <= max_val:
                                  print(f"  ✓ {code}: {val}% (country page, range {min_val}-{max_val}%)")
                                  return round(val, 3), str(date.today())
              except Exception as e:
                  print(f"  ❌ Country page fallback {code}: {e}")
              return None, None

          def scrape_bonds():
              print(f"\n{'='*50}\nBOND YIELDS: TradingEconomics (3-level + world)\n{'='*50}")
              data, dates = {}, {}

              try:
                  # Con ?continent=world: garantiza que NZD y todos los países aparecen
                  r = requests.get(TE_URLS['bond10y'], headers=HEADERS, timeout=20)
                  if r.status_code == 429:
                      print("  ⚠️ Rate limited, waiting 30s...")
                      time.sleep(30)
                      r = requests.get(TE_URLS['bond10y'], headers=HEADERS, timeout=20)
                  r.raise_for_status()
                  soup = BeautifulSoup(r.content, 'lxml')

                  # Nivel 1: class="table" (tabla estándar de TE)
                  std_table = soup.find('table', {'class': 'table'})
                  if std_table:
                      print("  Found class='table' — trying standard parser...")
                      headers_row = [h.get_text(strip=True).lower() for h in std_table.find_all('th')]
                      actual_idx = headers_row.index('actual') if 'actual' in headers_row else 1
                      date_idx   = headers_row.index('reference') if 'reference' in headers_row else None
                      for row in std_table.find_all('tr')[1:]:
                          cols = row.find_all('td')
                          if len(cols) < 2: continue
                          ctry = cols[0].get_text(strip=True)
                          for code, names in BOND_COUNTRY_MAP.items():
                              if any(n.lower() in ctry.lower() for n in names):
                                  val = clean_num(cols[actual_idx].get_text(strip=True))
                                  dt  = parse_te_date(cols[date_idx].get_text(strip=True)) if date_idx and len(cols) > date_idx else str(date.today())
                                  min_v, max_v = BOND_EXPECTED_RANGES.get(code, (0, 20))
                                  if val is not None and min_v <= val <= max_v:
                                      data[code] = round(val, 3)
                                      dates[code] = dt
                                      print(f"  ✓ {code}: {val}% (standard table)")
                                  break

                  # Nivel 2: Todas las tablas (widget table sin class)
                  missing = [c for c in CURRENCIES if c not in data]
                  if missing:
                      print(f"  Scanning all tables for: {', '.join(missing)}")
                      w_data, w_dates = _extract_bonds_from_all_tables(soup)
                      for code in missing:
                          if code in w_data:
                              # Verificar rango esperado
                              min_v, max_v = BOND_EXPECTED_RANGES.get(code, (0, 20))
                              if min_v <= w_data[code] <= max_v:
                                  data[code]  = w_data[code]
                                  dates[code] = w_dates[code]
                              else:
                                  print(f"  ⚠️ {code}: {w_data[code]}% fuera del rango esperado ({min_v}-{max_v}%) → descartado")

              except Exception as e:
                  print(f"  ❌ TE bond page error: {e}")

              # Nivel 3: Country-page fallback con rango estricto
              still_missing = [c for c in CURRENCIES if c not in data]
              if still_missing:
                  print(f"\n  Country-page fallback for: {', '.join(still_missing)}")
                  for code in still_missing:
                      val, dt = _bond_country_page_fallback(code)
                      if val is not None:
                          data[code]  = val
                          dates[code] = dt
                      time.sleep(1)

              found     = [c for c in CURRENCIES if c in data]
              not_found = [c for c in CURRENCIES if c not in data]
              print(f"\n  Bonds found:   {found}")
              print(f"  Bonds missing: {not_found}")
              return data, dates

          # ── MAIN ──────────────────────────────────────────────────────────────
          all_data  = {c: {} for c in CURRENCIES}
          all_dates = {c: {} for c in CURRENCIES}

          # 1. Bond yields
          bond_data, bond_dates = scrape_bonds()
          for c in CURRENCIES:
              all_data[c]['bond10y']  = bond_data.get(c)
              all_dates[c]['bond10y'] = bond_dates.get(c, str(date.today()))
          time.sleep(2)

          # 2-6. Sentiment + Flows (ya incluyen ?continent=world)
          for key, url in {k: v for k, v in TE_URLS.items() if k != 'bond10y'}.items():
              d, dt = scrape_te_table(url, key)
              for c in CURRENCIES:
                  all_data[c][key]  = d.get(c)
                  all_dates[c][key] = dt.get(c, str(date.today()))
              time.sleep(2)

          # Save + validation summary
          print(f"\n{'='*50}\nSAVING RESULTS\n{'='*50}")
          for curr in CURRENCIES:
              pkg = {
                  'lastUpdate': str(date.today()),
                  'source': 'TradingEconomics (?continent=world) — bonds+confidence fully normalized',
                  'data':   all_data[curr],
                  'dates':  all_dates[curr]
              }
              with open(f'extended-data/{curr}.json', 'w') as f:
                  json.dump(pkg, f, indent=2)
              available = [k for k, v in all_data[curr].items() if v is not None]
              missing   = [k for k in ['bond10y','consumerConfidence','businessConfidence',
                                        'capitalFlows','inflationExpectations'] if all_data[curr].get(k) is None]
              status = '✅' if not missing else '⚠️ '
              print(f"{status} {curr}: {', '.join(available) or 'NO DATA'}")
              if missing:
                  print(f"   Missing: {', '.join(missing)}")

          print("\n✅ COMPLETE")
          EOFPYTHON

      - name: Commit and push
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add extended-data/
          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "📊 Extended data $(date +'%Y-%m-%d') — world continent + bond range validation"
            git pull --rebase origin main || true
            git push
          fi
